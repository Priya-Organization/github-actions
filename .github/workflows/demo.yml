name: Demo
on:
  push:
    branches:
      - 'main'

jobs:
  CI_Steps:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install dependencies
        run : echo SAST Scans
        # .github/workflows/demo.yml@main